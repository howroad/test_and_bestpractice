//变更操作
function doChangeGua(obj){
	var tr = nstc.sf.findParent(obj, "TR");
	var contractId = $(tr).find("input[id$='.contractId']").val();
	
	//校验状态是否为已生效状态
	var data = validateContractState(contractId);
	if(data != null && data != ''){
		alert(data)
		return;
	}
	
	//校验是否在工作流中
	var result = validateInFlow('GDEBIT_FM005',contractId);
	if(result != '' && result != null && result != undefined){
		alert(result);
		return;
	}
	
	//var bussVariety = $(tr).find("input[id$='.bussVariety']").val();
	var url = 'GDEBIT@gdt_guaChangeInput.sf?m=e';
	
	var guaranteeType = $(tr).find("input[id$='.guaranteeType']").val();
	var occupyId = $(tr).find("input[id$='.occupyId']").val();
	var pledgeId = $(tr).find("input[id$='.pledgeId']").val();if(pledgeId ==undefined || pledgeId == "") pledgeId = null;
	url = url +"&contractId="+contractId+"&occupyId="+occupyId+"&pledgeId="+pledgeId+"&guaranteeType="+guaranteeType;
	openwin(url, '担保占用变更录入', 1250, 600);
}



//待处理记录
function showGuaWait(){
	var url = 'GDEBIT@gdt_guaChangeWait.sf';
	location.href=url;
}
