function callback(obj) {
	var value = obj.innerHTML;
	var tr = $(nstc.sf.findParent(obj, "TR"));
	var name = tr.find("input[id$='.gName']").val();
	var num=$('input[id$="num"]').val();
	var result = {};
	result.value = value;
	result.name = name;
	result.num=num;
	top.Layer.getWin().setGGdebitType(result);
	windowClose();
}